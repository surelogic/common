package com.surelogic.common.ref;

/**
 * Interface for declaration instances where {@link #getKind()} ==
 * {@link Kind#CLASS} or {@link #getKind()} == {@link Kind#ENUM} or
 * {@link #getKind()} == {@link Kind#INTERFACE}.
 */
public interface IDeclType extends IDecl {
  // marker interface
}
