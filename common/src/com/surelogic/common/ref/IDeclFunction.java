package com.surelogic.common.ref;

/**
 * Interface for declaration instances where {@link #getKind()} ==
 * {@link Kind#CONSTRUCTOR} or {@link #getKind()} == {@link Kind#METHOD}.
 */
public interface IDeclFunction extends IDecl {
  // marker interface
}
