package com.surelogic.common.ref;

import com.surelogic.common.ref.IDecl.Kind;

/**
 * Interface for declaration instances where {@link #getKind()} ==
 * {@link Kind#LAMBDA}.
 */
public interface IDeclLambda extends IDecl {
  // marker interface
}
