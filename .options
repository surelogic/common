# For this file to be examined, the Eclipse platform debug tracing facility
# must be turned on using the "-debug" option and the "-consoleLog" option on
# the Eclipse command line.
 
# Sets the level of java.util.logging messages that are directed to the Eclipse
# log.  The lowest logging level that equals "true" sets the output level,
# i.e., messages at that level and above are logged.  For example, if
# ".../debug/fine=false" and ".../debug/finer=true" then the former is ignored
# and all messages at FINER and above are logged.
com.surelogic.common.eclipse/fine=false
com.surelogic.common.eclipse/finer=false
com.surelogic.common.eclipse/finest=false
